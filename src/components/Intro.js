/*import profileImg from "./profile-img.png"
import { useState } from 'react'

const Intro = () => {
const [isScroll, setScroll] = useState();

const toggleClass = () => {
    setScroll(!isScroll);
  };

  console.log(isScroll);

return (
<div className="intro">
        <div className={isScroll ? "name-scroll" : "name"} onScroll={toggleClass}>
            <h1 className="name-text">LYDIA WELHAM</h1>
        </div>
        <div className="img-container">
            <h2 className="rotated-text">WEB DEVELOPER</h2>
            <img className="profile-img" src={profileImg} alt="Lydia is sat in a square in Oxford on a summy day" />
        </div>
    </div>
)
}

export default Intro */